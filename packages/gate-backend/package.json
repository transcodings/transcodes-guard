{
  "name": "@transcodes-guard/gate-backend",
  "version": "0.1.0",
  "description": "Private GateBackend implementation — binds stepup-core + danger-patterns + the Transcodes-backend MCP tools (src/mcp-tools/) into the public @transcodes-guard/gate-contract DI interface. Injected via setGateBackend() at plugin bootstrap (phase 1: bundled; phase 2: CDN-loaded).",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@transcodes-guard/gate-contract": "*",
    "@transcodes-guard/stepup-core": "*",
    "@transcodes-guard/danger-patterns": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "FSL-1.1-ALv2"
}
