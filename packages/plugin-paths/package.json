{
  "name": "@transcodes-guard/plugin-paths",
  "version": "0.1.0",
  "description": "Persistent data + cache directory resolution for transcodes-guard plugins. Single source of truth that resolves all plugin-managed state to ~/.transcodes/state/ across the 4 hosts (Claude Code / Codex / Antigravity / Cursor), with fail-open migration from the legacy ~/.claude/transcodes-guard + OS cache locations.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "FSL-1.1-ALv2"
}
