{
  "name": "@ai-action-tracker/danger-patterns",
  "version": "0.1.0",
  "description": "Bash danger-pattern + MCP tool-rule registry — shared system rules + user CRUD for ai-action-tracker plugins.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && mkdir -p dist/data && cp src/data/*.json dist/data/"
  },
  "dependencies": {
    "@ai-action-tracker/plugin-paths": "*",
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
